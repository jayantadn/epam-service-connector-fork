{
  "name": "ev-range-node-red-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Node-RED EV range extender dashboard with Zenoh bridge to VM ECUs",
  "scripts": {
    "bridge": "python3 zenoh_bus_bridge.py",
    "node-red": "./node_modules/.bin/node-red --userDir . --flowFile flows/ev-range-dashboard.json",
    "dashboard": "python3 zenoh_bus_bridge.py & BRIDGE_PID=$!; trap 'kill $BRIDGE_PID 2>/dev/null' EXIT INT TERM; node-red --userDir . --flowFile flows/ev-range-dashboard.json",
    "start": "npm run dashboard"
  },
  "dependencies": {
    "@flowfuse/node-red-dashboard": "^1.30.2",
    "node-red": "^4.1.10"
  }
}
